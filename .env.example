# TaskFlow Lite - Environment Variables Example

# Google OAuth Configuration (Get from: https://console.cloud.google.com/)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_API_KEY=your-google-api-key

# Cloudflare D1 Database
# Will be automatically configured via wrangler.jsonc
# No need to set manually for local development with --local flag

# Development Settings
NODE_ENV=development
PORT=4000

# Notes:
# - For Google Calendar API, enable "Google Calendar API" in Google Cloud Console
# - For Gmail API, enable "Gmail API" in Google Cloud Console
# - Set authorized redirect URIs to your domain (e.g., http://localhost:3000 for dev)
# - Client-side OAuth means these keys will be visible in the frontend code
# - Consider using Cloudflare Workers secrets for production deployment
